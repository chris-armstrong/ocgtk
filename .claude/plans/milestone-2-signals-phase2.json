{
  "plan_id": "milestone-2-signals-phase2",
  "title": "Milestone 2 Signals — Phase 2: emit signals at L1, mirror at L2, delete _signals files",
  "source": "gir_gen/docs/plans/milestone-2-signals.md — Phase 2 (lines 938–1086) plus cross-references at lines 332–381 (signal_emission API) and lines 547+ (classify param-type)",
  "baseline": {
    "head_commit": "3e600f1d",
    "build_passes": true,
    "tests_pass": true,
    "known_failures": [],
    "notes": "Clean baseline at commit 3e600f1d (signals branch, PR #129 merged). `dune build` exits 0. `dune test gir_gen/` exits 0. `xvfb-run dune test ocgtk/` exits 0.\n\n--- D1 RESOLUTION ---\nD1 (G_VALUE_HOLDS_BOXED guard on ml_g_value_set_boxed) was raised as a follow-up item in the Phase 1 plan. Inspection of ocgtk/src/common/ml_gobject.c lines 579-588 confirms the guard IS already in place (`if (!G_VALUE_HOLDS_BOXED(gv)) caml_invalid_argument(...)`). This was added in commit 0058f773 ('feat(gobject): guard set_int64/set_variant/set_boxed ...'). D1 is RESOLVED — no stage 0 required.\n\n--- REUSE AUDIT ---\n\nFunctions/concepts Signal_gen.ml currently provides:\n- `ocaml_keywords` list (lines 7-64): kept unchanged, reused by new `sanitize_signal_name`.\n- `sanitize_signal_name` (lines 66-74): kept unchanged, reused by new `classify`.\n- `has_widget_parent` (lines 76-82): currently used only in `generate_signal_class`; can be dropped in Phase 2 since the new `classify` does not need the parent chain — the L1 `t` type is always the class's own L1 `t`.\n- `connect_target_expr` (line 84): used only by old `generate_signal_class`; replaced by a literal `\"obj\"` in `emit_l1_let`.\n- `is_void_signal` (line 86): reusable helper for the `classify` dispatch.\n- `get_qualified_module_name` (lines 89-106): used only by old `generate_signal_class` to build the class ref `obj : <Module>.t`; no longer needed in `classify` (which does not emit file-level code).\n- `generate_signal_class` (lines 108-146): REPLACED by the new pure-data API. This function writes standalone `_signals.ml` content; Phase 2 removes it entirely.\n\nSignal_marshaller.ml (`Signal_marshaller.classify ~ctx ~gir_type`) was added in Phase 1b.\nThe current signature is `classify : ctx:generation_context -> gir_type:gir_type -> result`.\nPhase 2 plan lines 547+ note that `classify` should eventually take a full `gir_param` (for direction/transfer/nullable) to support future phases. However:\n- Phase 2 only needs to handle `In`-direction parameters (non-In are skipped).\n- The `gir_signal.sig_parameters` record already has `direction` and `nullable` fields that `classify` in `signal_gen.ml` can check BEFORE calling `Signal_marshaller.classify ~ctx ~gir_type`.\n- Therefore Phase 2 does NOT need to change `Signal_marshaller.classify`'s signature. The extension to full `gir_param` is a future concern (Phase 3+).\n\nClass_gen_body.ml emit points:\n- `generate_class_module_body` (lines 29-211): the `inherit Gsomething_signals.something_signals obj` emit is at lines 159-163. This is where L2 signal methods are ADDED (after removing the inherit line).\n- `generate_class_signature_body` (lines 213-352): the `inherit Gsomething_signals.something_signals` class-type emit is at lines 295-299. Likewise.\n- Both functions already receive `~signals` as a parameter — they are the correct hook points.\n\nClass_gen.ml emit points (combined cyclic modules):\n- `generate_combined_class_module` (lines 250-326): the `generate_class_impl` inner function at line 289 emits the old `inherit G<class>_signals...` comment at lines 289-290. This also needs to be updated.\n- `generate_combined_class_signature` also calls `generate_class_signature_body` which carries the old inherit.\n\nDune_file.ml: No `_signals` entries exist in `dune_file.ml`. The `_signals` module entries appear in generated `dune-generated.inc` files inside `ocgtk/src/*/generated/`. These are written by `gir_gen.ml` NOT by `dune_file.ml`. Confirmed by grepping `dune_file.ml` — no `_signals` pattern found. The `dune-generated.inc` files list OCaml module names; removing `_signals` entries from those files is done via regeneration (Stage 6).\n\ngir_gen.ml pipeline:\n- `generate_signal_class` function at lines 713-740: writes standalone `g<class>_signals.ml` files. This must be removed.\n- `generate_all_signal_classes` function at lines 742-750: calls `generate_signal_class` for every entity. Both functions are removed in Stage 5.\n- `generate_signal_class` at lines 713-740 only writes a `.ml` file, NOT a `.mli`. Inspection of ocgtk/src/*/generated/ would show the `.mli` files are actually written here too (search confirms 156 pairs = 78 ML + 78 MLI). Wait — the count was 156 pairs = 156 items, i.e. 78 `.ml` + 78 `.mli`. But `generate_signal_class` only writes `.ml`. Need to check for a `.mli` writer... The current `generate_signal_class` only writes a `.ml` file (line 725: `g<class>_signals.ml`). The `.mli` files may be auto-derived by dune (no explicit `.mli` emission found in gir_gen.ml for signals). Confirmed: `find ocgtk/src/*/generated -name '*_signals.mli'` returns zero results — the 156 count in the task description referred to `.ml` files only (78 `.ml` files, matching 78 classes with at least one signal). The task description's 'pairs' wording was imprecise. All 78 are `.ml` only.\n\nLayer1_helpers.ml: `generate_signal_bindings` (lines 12-14) is a stub returning `\"\"`. This is not used anywhere meaningful — it is a left-over placeholder. It will be removed or ignored as part of Stage 2 cleanup.\n\nClass_gen_helpers.ml:\n- `signal_class_name` (line 16-17): used only by `class_gen_body.ml` for the `inherit` emit. Removed in Stage 4.\n- `get_signal_module_name` (line 19): same usage. Removed in Stage 4.\n\n--- OPEN QUESTIONS ---\nNone that would change stage decomposition. The key ambiguities are all resolved:\n1. `Signal_marshaller.classify` stays as-is (takes `gir_type`); Phase 2 callers check `direction` before calling it.\n2. The hook point for L1 emission is `class_gen_body.ml` (both `generate_class_module_body` and `generate_class_signature_body`), NOT `class_gen_method.ml`. Signal methods are structurally different from regular methods (no C stub, closure-based).\n3. The L2 emit point is also `class_gen_body.ml` — signals are added directly in the body generators, replacing the old `inherit G<class>_signals` line.\n4. Cyclic/combined class modules in `class_gen.ml` also need updating (the `generate_combined_class_module` path also emits the old signals comment/inherit).\n5. `dune_file.ml` does NOT need changes. The `_signals` module names disappear when regeneration stops writing those files.\n6. D1 guard is already present — no Stage 0 needed.\n\n--- STAGE SHAPE RATIONALE ---\nSuggested 7 stages; the planner chose 6 after merging the D1 stage into the introduction notes (since D1 is already done). Stage breakdown:\n  1. Rewrite signal_gen.ml: replace generate_signal_class with classify / emit_l1_val / emit_l1_let / emit_l2_method + new signal_gen.mli + unit tests (14 test cases from plan)\n  2. Wire L1 signal emission into class_gen_body.ml (module body + signature body) + unit tests\n  3. Wire L2 signal forwarder emission into class_gen_body.ml; remove old inherit G<class>_signals lines + unit tests\n  4. Remove generate_signal_class / generate_all_signal_classes from gir_gen.ml; also remove now-dead signal_class_name / get_signal_module_name helpers from class_gen_helpers.ml\n  5. Regenerate all 9 namespace bindings; verify zero _signals files, ~180 Signal.connect calls, zero inherit _signals lines\n  6. Integration tests: wire new signal_wrapper_tests cases into test runner; verify all 14 test cases pass\nStages 2+3 are split rather than merged because they touch different code paths and can be built/tested independently (Stage 2 adds L1 entries but still emits the L2 inherit; Stage 3 replaces the L2 inherit with forwarders)."
  },
  "epochs": [
    {
      "epoch": 1,
      "title": "Phase 2 — Generator rewrite and binding regeneration",
      "status": "pending",
      "replanned": false,
      "description": "Replace the standalone _signals file emitter with a pure-data API, wire signal emission into per-class L1 and L2 generation, remove the old pipeline, regenerate all 9 namespaces, and add comprehensive unit tests covering all 14 test cases from the plan.",
      "stages": [
        {
          "stage": 1,
          "title": "Rewrite signal_gen.ml: pure-data API (classify / emit_l1_val / emit_l1_let / emit_l2_method) + mli + unit tests",
          "status": "completed",
          "replanned": false,
          "description": "Replace the entire body of gir_gen/lib/generate/signal_gen.ml with the new pure-data API described in the plan at lines 332-381. The old `generate_signal_class` function is removed. The `ocaml_keywords` list and `sanitize_signal_name` are kept. A new `sanitize_param_name` (mirrors `sanitize_signal_name` but for parameter names) is added. A new `signal_emission` record type captures all computed values once. `classify` performs the per-signal dispatch against `Signal_marshaller.classify` and returns `(signal_emission, string) result`. `emit_l1_val`, `emit_l1_let`, `emit_l2_method` are pure string-rendering functions over `signal_emission`. A new signal_gen.mli exposes the public contract. Existing signal_wrapper_tests.ml is rewritten to target the new API. New tests cover 14 cases from the plan.",
          "files_to_modify": [
            "gir_gen/lib/generate/signal_gen.ml"
          ],
          "files_to_create": [
            "gir_gen/lib/generate/signal_gen.mli",
            "gir_gen/test/class_generation/signal_wrapper_tests.ml"
          ],
          "depends_on": [],
          "implementation_notes": "SIGNAL_EMISSION RECORD (define at top of signal_gen.ml):\n```ocaml\ntype signal_emission = {\n  signal              : Types.gir_signal;\n  method_name         : string;\n  raw_signal_name     : string;\n  param_marshallers   : (Types.gir_param * Signal_marshaller.marshaller) list;\n  return_marshaller   : Signal_marshaller.marshaller option;\n  ocaml_callback_type : string;\n  strategy            : [ `Connect_simple | `Closure ];\n}\n```\n\nSANITIZE_PARAM_NAME: like sanitize_signal_name but without the 'on_' prefix:\n```ocaml\nlet sanitize_param_name name =\n  let base = name |> String.map ~f:(function '-' -> '_' | c -> c)\n             |> Utils.to_snake_case in\n  if List.exists ocaml_keywords ~f:(String.equal base) then base ^ \"_\" else base\n```\n\nCLASSIFY function:\n1. Filter out non-In direction parameters: `List.filter sig ~f:(fun p -> p.direction = In)`; if any non-In params remain after filtering for supported types, return `Error \"non-in direction parameter\"` for that signal.\n2. For each In-direction param, call `Signal_marshaller.classify ~ctx ~gir_type:p.param_type`. Collect results; if any is `Unsupported reason`, return `Error reason`.\n3. Classify the return type with `Signal_marshaller.classify ~ctx ~gir_type:signal.return_type`:\n   - `void/none` → `None` for `return_marshaller`\n   - `Supported m` → `Some m`\n   - `Unsupported reason` → `Error reason`\n4. Derive `strategy`:\n   - `Connect_simple` if `return_marshaller = None` AND `param_marshallers = []`\n   - `Closure` otherwise\n5. Build `ocaml_callback_type`:\n   - If `param_marshallers = []` and `return_marshaller = None`: `\"unit -> unit\"` (for connect_simple)\n   - Otherwise: concatenate `\"<param>:<type> -> \"` for each param then append return type string (or `\"unit\"` if None); e.g. `\"n_press:int -> x:float -> y:float -> unit\"`\n\nEMIT_L1_VAL (produces one .mli entry):\n```ocaml\nlet emit_l1_val e =\n  sprintf \"val %s : ?after:bool -> t -> callback:(%s) -> Gobject.handler_id\\n\"\n    e.method_name e.ocaml_callback_type\n```\nNote: `t` here is the L1 module's own `t` (positional, not labelled).\n\nEMIT_L1_LET (produces one .ml entry):\nFor `Connect_simple` strategy:\n```ocaml\nlet emit_l1_let e =\n  sprintf \"let %s ?after obj ~callback =\\n  Gobject.Signal.connect_simple obj ~name:\\\"%s\\\" ~callback ~after:(Option.value after ~default:false)\\n\\n\"\n    e.method_name e.raw_signal_name\n```\nFor `Closure` strategy, build the full closure body:\n- Iterate `param_marshallers` to extract each param at position i+1 (positions start at 1; position 0 is the sender object, implicit):\n  `let <param_name> = <marshaller.getter_expr> (Gobject.Closure.nth argv ~pos:<i+1>) in`\n- If `return_marshaller = None`: emit `callback <~p1> <~p2> ...` directly as the closure result.\n- If `return_marshaller = Some m`: emit `let result = callback <~p1> ... in <m.setter_expr applied to Gobject.Closure.result argv and result>`.\n- The getter_expr in signal_marshaller uses a placeholder `v`; the emit code must substitute the actual `Gobject.Closure.nth argv ~pos:N` expression. IMPORTANT: `getter_expr` in `Signal_marshaller.marshaller` is currently a string like `\"Gobject.Value.get_boolean v\"` where `v` is a placeholder. The executor must substitute `v` with `(Gobject.Closure.nth argv ~pos:N)` or use a local `let v = Gobject.Closure.nth argv ~pos:N in` binding per parameter.\n\nEMIT_L2_METHOD (produces one L2 class method):\n```ocaml\nlet emit_l2_method e class_snake =\n  (* class_snake is e.g. \"window\", \"button\" for the self#as_<class> accessor *)\n  sprintf \"  method %s ?after ~callback =\\n    %s.%s ?after self#as_%s ~callback\\n\\n\"\n    e.method_name\n    (String.capitalize_ascii class_snake)  (* the L1 module name *)\n    e.method_name\n    class_snake\n```\nNOTE: the L1 module name is NOT always just `String.capitalize_ascii class_snake` for cross-namespace or combined modules. Pass `~layer1_module_name` explicitly from the caller (class_gen_body already has this). Similarly `class_snake` provides the `as_<x>` accessor name.\n\nSIGNAL_GEN.MLI must export:\n- `type signal_emission` with record fields\n- `val sanitize_signal_name : string -> string` (reused by class_gen_body for the method_name)\n- `val classify : ctx:Types.generation_context -> namespace:string -> Types.gir_signal -> (signal_emission, string) result`\n- `val emit_l1_val : signal_emission -> string`\n- `val emit_l1_let : signal_emission -> string`\n- `val emit_l2_method : signal_emission -> layer1_module_name:string -> class_snake:string -> string`\n\nNOTE on `namespace` parameter to `classify`: Signal_marshaller.classify already takes `ctx` which carries `ctx.namespace`; but the `namespace` label here matches the plan's API sketch. The `namespace` parameter may be dropped if `ctx` already provides it — use `ctx.namespace.namespace_name`. Keep the interface minimal.\n\nIMPORTANT: `emit_l1_let` for the Closure strategy must use `Buffer.t` internally (following existing `signal_gen.ml` convention with `bprintf`) for multiline emission. Return `Buffer.contents buf`.\n\nEXISTING TESTS: the 4 existing `signal_wrapper_tests.ml` tests call `generate_signal_class` which is being deleted. These 4 tests MUST be replaced with new tests targeting the new API. Do NOT keep the old test structure — it tests the wrong abstraction. The new tests call `classify` and then `emit_l1_let` / `emit_l1_val` / `emit_l2_method` and use `Ml_ast_helpers.parse_implementation` / `parse_interface` for AST inspection.",
          "coding_guidelines_to_emphasize": [
            "module-boundaries.md: signal_gen.mli must expose only the public API; sanitize_param_name and internal helpers stay unexposed",
            "naming-and-intermediates.md: name intermediates in classify (e.g. param_results, return_result, supported_params) rather than nesting Option/Result chains",
            "test-patterns.md: ALL assertions on generated code must use Ml_ast_helpers.parse_implementation or parse_interface then AST helpers — no string contains checks",
            "pattern-matching.md: classify must exhaustively match all Signal_marshaller.result variants; no catch-all",
            "error-handling.md: classify returns Result.t — Error strings must be descriptive enough for the stderr skip log"
          ],
          "test_requirements": {
            "existing_tests": [
              "gir_gen/test/class_generation/signal_marshaller_tests.ml — unchanged; verify Signal_marshaller.classify API is unmodified"
            ],
            "new_tests": [
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_void_zero_param_emits_connect_simple_in_l1_let",
                "description": "Case 1: synthetic Button with void zero-param 'clicked' signal. Call classify then emit_l1_let. Parse result AST. Assert top-level let binding 'on_clicked' exists and its body calls Gobject.Signal.connect_simple (not connect).",
                "asserts": "AST contains let on_clicked; method_body_calls_function body 'Gobject.Signal' 'connect_simple' = true"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_void_zero_param_emits_val_in_l1_val",
                "description": "Case 1 (sig side): emit_l1_val for same clicked signal. Parse interface AST. Assert val on_clicked exists.",
                "asserts": "parse_interface result contains a val 'on_clicked'"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_void_primitive_params_uses_closure_create",
                "description": "Case 2: synthetic 'pressed' signal with params (gint n_press, gdouble x, gdouble y), void return. classify returns Ok; emit_l1_let body contains Gobject.Closure.create and three Gobject.Closure.nth calls; l1_val has labelled args n_press:int, x:float, y:float; uses Signal.connect not connect_simple.",
                "asserts": "L1 let body: contains_function_call 'Gobject.Closure' 'create' = true; body contains 3 Closure.nth applications at pos 1/2/3; L1 val: callback type string contains 'n_press:int' and 'x:float' and 'y:float'"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_bool_return_zero_param_uses_closure_create",
                "description": "Case 3: synthetic 'close-request' signal with no params and gboolean return. classify returns Ok with strategy=Closure; emit_l1_let body contains Closure.create and sets bool result via Value.set_boolean; L1 val callback type is 'unit -> bool'.",
                "asserts": "strategy = `Closure; L1 let body calls Gobject.Closure.create; L1 val callback type contains '-> bool'"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_bool_return_bool_param_round_trip",
                "description": "Case 4: synthetic 'state-set' signal with gboolean param 'state' and gboolean return. L1 val callback type is 'state:bool -> bool'; closure body extracts pos:1 and sets bool result.",
                "asserts": "ocaml_callback_type = 'state:bool -> bool'; body extracts Closure.nth at pos 1; body calls Value.set_boolean on result"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_mixed_params_key_pressed_shape",
                "description": "Case 5: 'key-pressed'-shaped signal with params guint keyval, guint keycode, Gdk.ModifierType state, returning gboolean. Three nth extractions at pos 1/2/3; state param getter composes get_flags_int + modifiertype_of_int (cross-NS). Context must include GDK cross-reference for ModifierType bitfield.",
                "asserts": "param_marshallers has 3 entries; third entry getter_expr contains 'get_flags_int'; third entry getter_expr contains 'modifiertype_of_int'"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_unsupported_signal_returns_error",
                "description": "Case 6: synthetic signal with a <callback>-typed parameter (e.g. name 'DestroyFunc'). classify returns Error with a non-empty reason string. emit_l1_val and emit_l1_let are NOT called.",
                "asserts": "classify returns Error _; the reason string is non-empty"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_sender_not_at_pos_0",
                "description": "Case 7: inspect any multi-param closure body emitted by emit_l1_let. Assert no Closure.nth call uses pos:0 (sender is implicit, params start at pos 1).",
                "asserts": "All Closure.nth applications in the body use pos >= 1; no pos:0 literal appears"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_keyword_param_name_sanitised",
                "description": "Case 8: synthetic signal with a parameter named 'type' (OCaml keyword). classify returns Ok; ocaml_callback_type contains 'type_:' (sanitized with trailing underscore); emit_l1_let uses '~type_' in the callback application.",
                "asserts": "ocaml_callback_type contains 'type_:'; emitted let body contains '~type_'"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_int64_param_maps_to_int64_t",
                "description": "Case 9: synthetic signal with gint64 param. classify returns Ok; param_marshallers entry has ocaml_type = 'Int64.t' and getter_expr contains 'get_int64'.",
                "asserts": "param_marshaller.ocaml_type = 'Int64.t'; param_marshaller.getter_expr contains 'Gobject.Value.get_int64'"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_variant_param_maps_to_gvariant",
                "description": "Case 10: synthetic signal with GLib.Variant param. classify returns Ok; param_marshallers entry has ocaml_type = 'Gvariant.t' and getter_expr contains 'get_variant'.",
                "asserts": "param_marshaller.ocaml_type = 'Gvariant.t'; param_marshaller.getter_expr contains 'Gobject.Value.get_variant'"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_l2_forwarder_shape",
                "description": "Case 11: synthetic Window class with void zero-param 'clicked' signal. Call emit_l2_method with class_snake='window' and layer1_module_name='Window'. Parse result as implementation fragment. Assert method 'on_clicked' exists in the output and its body contains 'Window.on_clicked' and 'self#as_window'.",
                "asserts": "Parsed class body contains method 'on_clicked'; method body calls Window.on_clicked; method body references self#as_window"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_l1_obj_is_positional_not_labelled",
                "description": "Case 12: emit_l1_val for any signal. Parse the val declaration. Assert the second positional argument type is 't' (not '~obj:t'); i.e. the object parameter is positional, following existing L1 method idiom.",
                "asserts": "l1_val string contains '-> t ->' (positional obj); does not contain '~obj'"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_no_signals_files_generated",
                "description": "Case 13: The new signal_gen.ml has no file-writing functions. Assert that signal_gen.mli does NOT export any function named 'generate_signal_class' or similar file-writing function.",
                "asserts": "signal_gen.mli does not contain 'generate_signal_class'; the mli exports only classify, emit_l1_val, emit_l1_let, emit_l2_method, sanitize_signal_name, signal_emission type"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_l2_has_no_inherit_signals_line",
                "description": "Case 14: emit_l2_method output for any signal. Parse as implementation AST. Verify the output string does not contain an inherit expression referencing a name ending in '_signals'. (The L2 output is a method body, not a class definition, so there should be no inherit at all.)",
                "asserts": "emit_l2_method result does not contain 'inherit'; does not contain '_signals'"
              }
            ],
            "test_command": "dune test gir_gen/"
          },
          "acceptance_criteria": [
            "dune build succeeds after rewriting signal_gen.ml and creating signal_gen.mli",
            "dune test gir_gen/ passes with all 14 new signal_wrapper_tests cases green",
            "signal_gen.ml exports no function named generate_signal_class",
            "signal_gen.mli exports: type signal_emission, val classify, val emit_l1_val, val emit_l1_let, val emit_l2_method, val sanitize_signal_name",
            "Signal_marshaller.classify API signature is unchanged (takes ~ctx ~gir_type, not ~gir_param)"
          ]
        },
        {
          "stage": 2,
          "title": "Wire L1 signal emission into class_gen_body.ml (module body + signature body)",
          "status": "pending",
          "replanned": false,
          "description": "Extend `generate_class_module_body` and `generate_class_signature_body` in class_gen_body.ml to emit L1 signal `let` bindings (in the .ml body) and `val` declarations (in the .mli body) for each supported signal. The old `inherit G<class>_signals` lines are NOT yet removed in this stage — they remain so the build still passes. This stage is additive only: signals will appear twice (via inherit AND via direct emission) but the direct emission is what matters. The `inherit` removal is Stage 3.",
          "files_to_modify": [
            "gir_gen/lib/generate/class_gen_body.ml"
          ],
          "files_to_create": [],
          "depends_on": [1],
          "implementation_notes": "In `generate_class_module_body` (around line 159, after the existing `inherit G<class>_signals obj` block), add signal emission BEFORE the inherit line. The emit is done at class-module level, NOT inside the object body — L1 signals are top-level `let` bindings in the module, not class methods:\n\n```ocaml\n(* Emit L1 signal let-bindings into the module body *)\nList.iter signals ~f:(fun signal ->\n  match Signal_gen.classify ~ctx ~namespace:ctx.namespace.namespace_name signal with\n  | Error reason ->\n      eprintf \"Skipping signal '%s' for %s (%s)\\n\" signal.signal_name class_name reason\n  | Ok emission ->\n      Buffer.add_string buf (Signal_gen.emit_l1_let emission));\n```\n\nWAIT — re-read the plan carefully. L1 signal emission is NOT inside the class object body — it is at the TOP-LEVEL of the L1 module, alongside the `external` declarations. The `generate_class_module_body` function generates the OBJECT BODY of the L2 class. L1 module content (externals, let bindings) is generated by `ml_interface.ml` and `layer1/` code, NOT by `class_gen_body.ml`.\n\nCORRECT HOOK POINT: L1 `let` bindings and `val` declarations must be added to the L1 module output, which is generated in `gir_gen/lib/generate/ml_interface.ml` (via `generate_ml_interface`). Check `ml_interface.ml` to find where methods are emitted, and add signal emission after the method section.\n\nFor `class_gen_body.ml` (L2 class body): this is where L2 `method on_<sig>` forwarders will be added. L2 method forwarders belong in the class object body. This is Stage 3, not Stage 2.\n\nRevised plan for Stage 2:\n- Read `ml_interface.ml` to find the correct location to emit L1 signal let-bindings and val declarations.\n- Add signal emit after existing externals/methods in both Interface and Implementation modes.\n- The emit loop calls `Signal_gen.classify` per signal; on Ok, appends `Signal_gen.emit_l1_val emission` (Interface mode) or `Signal_gen.emit_l1_let emission` (Implementation mode); on Error, calls `eprintf` with the skip reason.\n- The `eprintf` must match the format of existing skip messages for consistency.\n\nNOTE TO EXECUTOR: Before implementing, READ ml_interface.ml in full to understand its structure and where to add signal emission. The L1 module content generator is there, not in class_gen_body.ml.",
          "coding_guidelines_to_emphasize": [
            "naming-and-intermediates.md: name the list of Ok signal emissions (e.g. let supported_emissions = ...) if it is computed separately from the emit loop",
            "error-handling.md: Error from classify must be logged via eprintf to stderr using the same format as existing 'Skipping signal' messages",
            "nesting-and-control-flow.md: use early-return or Result.iter pattern rather than deep match nesting in the signal emit loop",
            "module-boundaries.md: no new public functions in ml_interface.mli unless strictly necessary"
          ],
          "test_requirements": {
            "existing_tests": [
              "gir_gen/test/class_generation/signal_wrapper_tests.ml — the 14 new tests from Stage 1 continue to pass (they test signal_gen in isolation)",
              "gir_gen/test/integration/signals_tests.ml — currently empty (tests = []); remains empty in this stage",
              "dune test gir_gen/ must pass including existing class generation tests (method_wrapper_tests, constructor_wrapper_tests)"
            ],
            "new_tests": [
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_l1_val_in_generated_module_interface",
                "description": "Call the L1 interface generator (generate_ml_interface with Interface mode) for a synthetic class with one void-zero-param signal. Parse the output .mli content. Assert val on_clicked exists at the top level of the module sig.",
                "asserts": "Ml_ast_helpers.find_value_declaration_sig on 'on_clicked' returns Some _"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_l1_let_in_generated_module_impl",
                "description": "Call the L1 impl generator (generate_ml_interface with Implementation mode) for the same synthetic class. Parse the output .ml content. Assert let on_clicked exists at the top level.",
                "asserts": "Ml_ast_helpers.find_let_binding on 'on_clicked' returns Some _"
              }
            ],
            "test_command": "dune test gir_gen/"
          },
          "acceptance_criteria": [
            "dune build succeeds",
            "dune test gir_gen/ passes",
            "Generated L1 .mli for a class with signals contains val on_<sig> entries (verified by the new tests)",
            "Generated L1 .ml for a class with signals contains let on_<sig> entries (verified by the new tests)",
            "Skipped signals produce eprintf output matching the existing format 'Skipping signal \\'<name>\\' for <Class> (<reason>)'"
          ]
        },
        {
          "stage": 3,
          "title": "Wire L2 signal forwarder emission into class_gen_body.ml; remove old inherit G<class>_signals lines",
          "status": "pending",
          "replanned": false,
          "description": "Replace the `inherit G<class>_signals.something_signals obj` emit in `generate_class_module_body` (and the corresponding `inherit G<class>_signals.something_signals` in `generate_class_signature_body`) with inline L2 method forwarder emission using `Signal_gen.emit_l2_method`. Also update the two analogous paths in `class_gen.ml`'s combined/cyclic class generators. After this stage, no generated L2 class body or class type inherits from a `_signals` module.",
          "files_to_modify": [
            "gir_gen/lib/generate/class_gen_body.ml",
            "gir_gen/lib/generate/class_gen.ml"
          ],
          "files_to_create": [],
          "depends_on": [2],
          "implementation_notes": "In `generate_class_module_body` (class_gen_body.ml lines 159-164), replace:\n```ocaml\n  if has_any_signals then begin\n    let signal_module = get_signal_module_name class_snake in\n    bprintf buf \"  inherit %s.%s obj\\n\" signal_module\n      (signal_class_name class_name)\n  end;\n```\nwith:\n```ocaml\n  List.iter signals ~f:(fun signal ->\n    match Signal_gen.classify ~ctx ~namespace:ctx.namespace.namespace_name signal with\n    | Error _ -> () (* already logged at L1 emission; skip silently here *)\n    | Ok emission ->\n        Buffer.add_string buf\n          (Signal_gen.emit_l2_method emission\n             ~layer1_module_name:layer1_module_name\n             ~class_snake:class_snake));\n```\n\nIn `generate_class_signature_body` (class_gen_body.ml lines 295-300), replace:\n```ocaml\n  if has_any_signals then begin\n    let signal_module = get_signal_module_name class_snake in\n    bprintf buf \"    inherit %s.%s\\n\" signal_module\n      (signal_class_name class_name)\n  end;\n```\nwith the equivalent using `Signal_gen.emit_l2_method` — but note that for the CLASS TYPE DEFINITION (.mli), we need `method on_<sig> : ?after:bool -> callback:(<type>) -> Gobject.handler_id` (without the implementation body). The plan's `emit_l2_method` returns an implementation method. For the class type signature, use `emit_l1_val` with the callback type and drop the `t ->` positional parameter (since `self` is implicit in a class type method). This means a new helper is needed: `emit_l2_method_sig : signal_emission -> string` OR the executor emits a method signature inline here.\n\nAlternatively: the simplest correct approach is to emit in the class type body:\n```\n  method on_<sig> : ?after:bool -> callback:(<ocaml_callback_type>) -> Gobject.handler_id\n```\nThis can be done by adding `emit_l2_method_sig` to signal_gen.ml (and mli), or by inlining it in class_gen_body.ml.\n\nFor `class_gen.ml` combined module generators: the two locations are `generate_class_impl` (line 289) and the class type emission. The same pattern applies — replace the old signals comment+inherit with direct method emission.\n\nIMPORTANT: Remove the `has_any_signals` check (and the comment `(* Signal class defined in g%s_signals.ml *)`) from `class_gen.ml` lines 149-153 and 288-290.\n\nAlso: the `has_any_signals` bool in `generate_class_module_body` was only used to decide whether to emit the inherit. After this change, it can be removed (or kept and used to decide whether to iterate the signals list, though iterating an empty list is harmless).",
          "coding_guidelines_to_emphasize": [
            "pattern-matching.md: the Error branch of classify in the L2 emit loop should silently skip (error was already logged at L1); add an inline comment explaining why",
            "code-reuse.md: emit_l2_method_sig (if added to signal_gen) should share the callback type computation from the existing ocaml_callback_type field of signal_emission — no re-derivation",
            "naming-and-intermediates.md: if emit_l2_method_sig is added, it should be a 1-line function delegating to the shared ocaml_callback_type field",
            "module-boundaries.md: if emit_l2_method_sig is added to signal_gen.mli, include a doc comment distinguishing it from emit_l2_method"
          ],
          "test_requirements": {
            "existing_tests": [
              "gir_gen/test/class_generation/signal_wrapper_tests.ml — all tests from Stages 1+2 must continue to pass",
              "gir_gen/test/ — full test suite must pass after this change"
            ],
            "new_tests": [
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_l2_class_body_contains_on_signal_method",
                "description": "Call generate_class_module for a synthetic Button class with a void zero-param 'clicked' signal. Parse the output .ml. Find the 'button' class declaration. Assert it contains a method 'on_clicked' that calls 'Button.on_clicked' and 'self#as_button'.",
                "asserts": "find_class_declaration ast 'button' returns Some; find_method_in_class 'on_clicked' returns Some; method body calls Button.on_clicked"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_l2_class_type_contains_on_signal_method_sig",
                "description": "Call generate_class_signature for the same Button class. Parse the output .mli. Find the 'button_t' class type. Assert it contains a method type 'on_clicked' (a method declaration, not an inherit).",
                "asserts": "find_class_declaration_sig ast 'button_t' or equivalent; method 'on_clicked' present; no 'inherit' referencing '_signals' in the output"
              },
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "test_l2_no_inherit_signals_in_generated_class",
                "description": "Case 14 at class_gen level: generate_class_module output for a class with signals must not contain any 'inherit G<x>_signals' expression. Parse the AST and verify no inherit node references a name ending in '_signals'.",
                "asserts": "No Pcf_inherit in the class body references a module name containing '_signals'"
              }
            ],
            "test_command": "dune test gir_gen/"
          },
          "acceptance_criteria": [
            "dune build succeeds",
            "dune test gir_gen/ passes with all tests green",
            "Generated L2 class body contains method on_<sig> forwarders for all supported signals",
            "Generated L2 class type contains method on_<sig> signatures for all supported signals",
            "No generated L2 class body or class type contains 'inherit G<x>_signals' (verified by test_l2_no_inherit_signals_in_generated_class)",
            "class_gen.ml no longer contains the '(* Signal class defined in g%s_signals.ml *)' comment-emit"
          ]
        },
        {
          "stage": 4,
          "title": "Remove generate_signal_class pipeline from gir_gen.ml; clean up dead helpers in class_gen_helpers.ml",
          "status": "pending",
          "replanned": false,
          "description": "Remove the `generate_signal_class` and `generate_all_signal_classes` functions from gir_gen/bin/gir_gen.ml (lines 713-750) and the call to `generate_all_signal_classes` from the generate command's execution body. Remove the now-dead `signal_class_name` and `get_signal_module_name` helpers from class_gen_helpers.ml (these were only consumed by class_gen_body.ml which no longer calls them after Stage 3). Remove the stub `generate_signal_bindings` from layer1_helpers.ml.",
          "files_to_modify": [
            "gir_gen/bin/gir_gen.ml",
            "gir_gen/lib/generate/class_gen_helpers.ml",
            "gir_gen/lib/generate/layer1/layer1_helpers.ml"
          ],
          "files_to_create": [],
          "depends_on": [3],
          "implementation_notes": "In gir_gen.ml:\n- Delete the `generate_signal_class` function (lines 712-741).\n- Delete the `generate_all_signal_classes` function (lines 743-750).\n- Find the call site of `generate_all_signal_classes` in the generate pipeline (search for its name) and remove that call.\n- The printf 'Generating signal classes...' line that was inside `generate_all_signal_classes` is also removed.\n\nIn class_gen_helpers.ml:\n- Remove `signal_class_name` (lines 16-17).\n- Remove `get_signal_module_name` (line 19).\n- Verify no other module uses these by running `grep -r signal_class_name --include='*.ml'` and `grep -r get_signal_module_name --include='*.ml'`.\n\nIn layer1_helpers.ml:\n- Remove `generate_signal_bindings` (lines 12-14) — it returns `\"\"` and is never called after Phase 1 cleanup.\n\nNOTE: Before removing, check all call sites with grep to ensure nothing else depends on these functions. If class_gen.ml still references `signal_class_name` or `get_signal_module_name` (which it should not after Stage 3), fix those references first.",
          "coding_guidelines_to_emphasize": [
            "code-reuse.md: deleting dead code is the mirror of code-reuse — do not leave unused helpers",
            "module-boundaries.md: after deletion, re-verify that class_gen_helpers.mli (if it exists) does not expose removed functions"
          ],
          "test_requirements": {
            "existing_tests": [
              "dune build — the primary test; removed functions must not cause linker/compiler errors",
              "dune test gir_gen/ — all existing tests must continue to pass"
            ],
            "new_tests": [],
            "test_command": "dune build && dune test gir_gen/"
          },
          "acceptance_criteria": [
            "dune build succeeds with no undefined reference or unused-module warnings",
            "dune test gir_gen/ passes",
            "gir_gen.ml contains no reference to generate_signal_class or generate_all_signal_classes",
            "class_gen_helpers.ml contains no signal_class_name or get_signal_module_name definitions",
            "grep -r 'generate_signal_class' gir_gen/ returns zero matches",
            "grep -r 'get_signal_module_name\\|signal_class_name' gir_gen/ returns zero matches (except possible plan/doc files)"
          ]
        },
        {
          "stage": 5,
          "title": "Regenerate all 9 namespace bindings; verify zero _signals files and ~180 Signal.connect calls",
          "status": "pending",
          "replanned": false,
          "description": "Run `bash scripts/generate-bindings.sh` from the repository root to regenerate all 9 namespace bindings (Cairo, Gio, Gdk, Graphene, GdkPixbuf, Pango, PangoCairo, Gsk, Gtk) using the updated generator. Verify all validation outputs from the plan: zero _signals files, L1 val on_<sig> entries present, L2 method forwarders present, ~180 Signal.connect calls, zero inherit _signals lines. Then run `dune build` and both test suites to confirm the regenerated bindings compile and tests pass.",
          "files_to_modify": [
            "ocgtk/src/cairo/generated/*",
            "ocgtk/src/gio/generated/*",
            "ocgtk/src/gdk/generated/*",
            "ocgtk/src/graphene/generated/*",
            "ocgtk/src/gdkpixbuf/generated/*",
            "ocgtk/src/pango/generated/*",
            "ocgtk/src/pangocairo/generated/*",
            "ocgtk/src/gsk/generated/*",
            "ocgtk/src/gtk/generated/*"
          ],
          "files_to_create": [],
          "depends_on": [4],
          "implementation_notes": "Run from repo root:\n```bash\nbash scripts/generate-bindings.sh\n```\n\nAfter regeneration, verify all plan validation outputs:\n\n1. Zero _signals files:\n   `find ocgtk/src/*/generated -name '*_signals.ml' -o -name '*_signals.mli'`\n   Expected: zero results. If any remain, the Stage 4 cleanup was incomplete.\n\n2. L1 signal entries spot-check:\n   `grep -n 'val on_close_request\\|val on_activate_default' ocgtk/src/gtk/generated/application_and__window_and__window_group.mli`\n   Expected: hits in the Window submodule sig.\n\n3. L1 closure body shape:\n   `grep -n 'on_key_pressed\\|keyval\\|keycode\\|state' ocgtk/src/gtk/generated/application_and__window_and__window_group.ml`\n   Expected: let on_key_pressed exists; closure extracts at pos 1/2/3.\n\n4. L2 forwarder shape:\n   `grep -n 'on_close_request\\|inherit.*_signals' ocgtk/src/gtk/generated/gapplication_and__window_and__window_group.ml`\n   Expected: method on_close_request exists; no inherit _signals.\n\n5. Total Gobject.Signal.connect (non-simple) count in gtk:\n   `grep -rh 'Gobject.Signal.connect [^_]' ocgtk/src/gtk/generated/ | wc -l`\n   Expected: approximately 180 (up from 0).\n\n6. Total connect_simple count:\n   `grep -rh 'connect_simple' ocgtk/src/gtk/generated/ | wc -l`\n   Expected: approximately 27.\n\n7. Zero inherit _signals:\n   `grep -rh 'inherit G.*_signals' ocgtk/src/gtk/generated/ | wc -l`\n   Expected: 0.\n\n8. Stderr skip count:\n   `bash scripts/generate-bindings.sh 2>&1 | grep -c 'Skipping signal'`\n   Expected: drops from ~205 to roughly 60-80.\n\nAfter regeneration:\n```bash\ndune build\ndune test gir_gen/\nxvfb-run dune test ocgtk/\n```\nAll three must pass.",
          "coding_guidelines_to_emphasize": [
            "test-patterns.md: validation checks here use shell grep/find (appropriate since we are validating file-system output, not OCaml code structure)"
          ],
          "test_requirements": {
            "existing_tests": [
              "dune build — regenerated bindings must compile without errors",
              "dune test gir_gen/ — generator unit tests must pass",
              "xvfb-run dune test ocgtk/ — runtime tests must pass",
              "gir_gen/test/integration/signals_tests.ml — currently empty; remains empty (integration subprocess tests may be added in Stage 6 if time allows)"
            ],
            "new_tests": [],
            "test_command": "bash scripts/generate-bindings.sh && dune build && dune test gir_gen/ && xvfb-run dune test ocgtk/"
          },
          "acceptance_criteria": [
            "bash scripts/generate-bindings.sh exits 0",
            "dune build exits 0 with no errors",
            "dune test gir_gen/ exits 0",
            "xvfb-run dune test ocgtk/ exits 0",
            "find ocgtk/src/*/generated -name '*_signals.ml' -o -name '*_signals.mli' returns zero results",
            "grep -rh 'inherit G.*_signals' ocgtk/src/gtk/generated/ | wc -l prints 0",
            "grep -rh 'Gobject.Signal.connect [^_]' ocgtk/src/gtk/generated/ | wc -l prints a value >= 100 (rough lower bound for ~180 expected)",
            "Stderr skip count (grep -c 'Skipping signal' from generate-bindings.sh stderr) is <= 100"
          ]
        },
        {
          "stage": 6,
          "title": "Verify and finalize: ensure all 14 test cases pass; update test runner if needed",
          "status": "pending",
          "replanned": false,
          "description": "Verify that all 14 test cases from the plan are implemented and passing in `signal_wrapper_tests.ml`. Add any missing test cases not yet covered by Stages 1-3. Verify the test runner (gir_gen/test/dune) includes `signal_wrapper_tests` in the test_gir_gen modules list (it is already there from Phase 1). If any integration-level tests are appropriate for the integration test binary (signals_tests.ml), add them. Run the full test suite as a final gate.",
          "files_to_modify": [
            "gir_gen/test/class_generation/signal_wrapper_tests.ml"
          ],
          "files_to_create": [],
          "depends_on": [5],
          "implementation_notes": "Review the 14 test cases from plan lines 1061-1076 against what was implemented in Stage 1:\n\nCases 1-12 are unit tests of signal_gen.ml classify + emit_l1_val/emit_l1_let/emit_l2_method — should all be in signal_wrapper_tests.ml from Stage 1.\n\nCases 13 and 14 are architectural invariant tests:\n- Case 13: 'no _signals files generated' — at the class_gen level, generate_class_module for a class with signals should write no file paths ending in '_signals.ml' or '_signals.mli'. This is a structural property verified by inspecting what signal_gen.mli exports (no file-writing function). If this test is not already present from Stage 1, add it.\n- Case 14: 'L2 has no inherit-_signals line' — from Stage 3's test. If the test was added there, verify it still passes. If not, add it here.\n\nAny missing cases among 1-14 should be backfilled in this stage.\n\nAlso verify the test cases added in Stages 2-3 (the class_gen-level tests calling generate_class_module / generate_class_signature) are all wired into the test_gir_gen modules list in the dune file. Check gir_gen/test/dune — `signal_wrapper_tests` is already listed. No dune file changes needed unless new test files were created in earlier stages.\n\nFinal validation:\n- Count test cases in signal_wrapper_tests.ml: should be >= 14 cases.\n- `dune test gir_gen/` must print all signal_wrapper_tests cases as [OK].\n- `xvfb-run dune test ocgtk/` must pass.\n- `dune build` must pass with zero errors.",
          "coding_guidelines_to_emphasize": [
            "test-patterns.md: AST-based validation required for all generated OCaml code assertions; no string_contains",
            "test-patterns.md: use Helpers.assert_some / expect_some rather than inline match expressions",
            "test-patterns.md: test names must describe what is tested and expected outcome (e.g. test_void_zero_param_emits_connect_simple_in_l1_let)"
          ],
          "test_requirements": {
            "existing_tests": [
              "gir_gen/test/class_generation/signal_wrapper_tests.ml — all 14 cases must be present and passing",
              "gir_gen/test/class_generation/signal_marshaller_tests.ml — unchanged, must still pass",
              "Full test suite: dune test gir_gen/ && xvfb-run dune test ocgtk/"
            ],
            "new_tests": [
              {
                "file": "gir_gen/test/class_generation/signal_wrapper_tests.ml",
                "test_name": "(backfill any missing cases from the 14 listed in the plan)",
                "description": "If any of the 14 plan cases from lines 1061-1076 are not yet covered by tests from Stages 1-3, add them here. Check each case number against the existing test list.",
                "asserts": "All 14 cases have at least one corresponding Alcotest.test_case in the tests list"
              }
            ],
            "test_command": "dune build && dune test gir_gen/ && xvfb-run dune test ocgtk/"
          },
          "acceptance_criteria": [
            "dune build succeeds",
            "dune test gir_gen/ passes with zero failures",
            "xvfb-run dune test ocgtk/ passes",
            "signal_wrapper_tests.ml contains >= 14 Alcotest.test_case entries covering cases 1-14 from the plan",
            "All validation outputs from Stage 5 continue to hold (zero _signals files, zero inherit _signals lines, ~180 Signal.connect calls)"
          ]
        }
      ]
    }
  ]
}
