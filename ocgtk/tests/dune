; Test suite for ocgtk
;

; DISABLED: These tests use the Conv module which is not generated by gir_gen
; The Conv module was a legacy feature that provided OCaml-level encoder/decoder
; functions. gir_gen enums are type-only; conversion is handled at the C level.
;
;(test
; (name test_enum_roundtrip)
; (modules test_enum_roundtrip)
; (libraries ocgtk alcotest))
;
;(test
; (name test_enum_values)
; (modules test_enum_values)
; (libraries ocgtk alcotest))
;
;(test
; (name test_all_enums)
; (modules test_all_enums)
; (libraries ocgtk alcotest))

; DISABLED: Uses Conv module (see above)
;(test
; (name test_glib)
; (modules test_glib)
; (libraries ocgtk alcotest))

; DISABLED: Uses Conv module (see above)
;(test
; (name test_gobject)
; (modules test_gobject)
; (libraries ocgtk alcotest))

(test
 (name test_gobject_stress)
 (modules test_gobject_stress)
 (libraries ocgtk alcotest))

; DISABLED: Uses Conv module (see above)
;(test
; (name test_ffi_integration)
; (modules test_ffi_integration)
; (libraries ocgtk alcotest))

; (test
;  (name test_gdkpixbuf)
;  (modules test_gdkpixbuf)
;  (libraries ocgtk alcotest))

; DISABLED: Uses Conv module (see above)
;(test
; (name test_gdk)
; (modules test_gdk)
; (libraries ocgtk alcotest))

; (test
;  (name test_pango)
;  (modules test_pango)
;  (libraries ocgtk alcotest))

; (test
;  (name test_clipboard)
;  (modules test_clipboard)
;  (libraries ocgtk alcotest))

; Tests moved to gtk/ subdirectory
; (test
;  (name test_widget)
;  (modules test_widget)
;  (libraries ocgtk.gtk alcotest))

; (test
;  (name test_snapshot)
;  (modules test_snapshot)
;  (libraries ocgtk alcotest))

(test
 (name test_closure_stress)
 (modules test_closure_stress)
 (libraries ocgtk))

(test
 (name test_closure_with_gc)
 (modules test_closure_with_gc)
 (libraries ocgtk))

(test
 (name test_gtk_init)
 (modules test_gtk_init)
 (libraries ocgtk alcotest))

; Some APIs missing / need to be generated
; (test
;  (name test_event_controller)
;  (modules test_event_controller)
;  (libraries ocgtk alcotest))

; problem in GMain initialisation (is it required?)
; (test
;  (name test_event_controller_runtime)
;  (modules test_event_controller_runtime)
;  (libraries ocgtk alcotest))

; (test
;  (name test_gobj)
;  (modules test_gobj)
;  (libraries ocgtk ocgtk alcotest))

(test
 (name test_integration)
 (modules test_integration)
 (libraries ocgtk alcotest))

; Tests moved to gtk/ subdirectory
; (test
;  (name test_box)
;  (modules test_box)
;  (libraries ocgtk.gtk alcotest))

; Some APIs missing / need to be generated
; (test
;  (name test_grid)
;  (modules test_grid)
;  (libraries ocgtk alcotest))

; Some APIs missing / need to be generated
; (test
;  (name test_containers)
;  (modules test_containers)
;  (libraries ocgtk alcotest))

; DISABLED: Uses GPack module which needs to be rewritten for new API structure
;(test
; (name test_gpack)
; (modules test_gpack)
; (libraries ocgtk alcotest))

; DISABLED: Uses GWindow module which needs to be rewritten for new API structure
;(test
; (name test_window)
; (modules test_window)
; (libraries ocgtk alcotest))

; Tests moved to gtk/ subdirectory
; (test
;  (name test_button)
;  (modules test_button)
;  (libraries ocgtk.gtk alcotest))

; (test
;  (name test_range)
;  (modules test_range)
;  (libraries ocgtk.gtk alcotest))
