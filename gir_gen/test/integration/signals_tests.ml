(* Signal generation integration tests.
   Unit tests (in-process generation) moved to
   class_generation/signal_wrapper_tests.ml (Phase 2.2).
   Subprocess integration tests can be added here when needed. *)

let tests = []
